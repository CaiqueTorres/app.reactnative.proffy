import React from 'react'
import { RectButton } from 'react-native-gesture-handler'

const Button = () => {
    
    
    return (
        <RectButton>
            
        </RectButton>
    )
}

export default Button
